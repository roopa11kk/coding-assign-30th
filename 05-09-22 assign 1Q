#include<stdio.h>
#include<conio.h>
void main()
{
	int m,n;
	scanf("%d %d",&m,&n);
	display(m,n);
}
void display(int start,int end)
{
	if(start>end)
	return;
	else
	printf("%d\n",start);
	dispaly(start+1,end);
}
